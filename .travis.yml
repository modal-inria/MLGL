language: r
cache: packages
cran: http://cran.rstudio.com
dist: bionic

os:
  - linux
  - osx
  
r:
  - oldrel
  - release
  - devel

jobs:
  exclude:
  - r: devel
    os: osx
   
script:
  - R CMD build .
  - R CMD check *.tar.gz --as-cran
  
